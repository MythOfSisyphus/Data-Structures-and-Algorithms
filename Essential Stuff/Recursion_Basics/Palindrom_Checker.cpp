/*
Write a recursive function to check if a given string is a palindrome.
*/

#include<bits/stdc++.h>

using namespace std;

bool CheckingIt(char[] input){
    int input_len = strlen(input);
    return true;
}

int main(){

    char lucky[] = "useful";
    int x = strlen(lucky);

    return 0;
}